
import styled from "styled-components";

export const DrPlaceholder = styled.h3`
    font-size: 17px;
    font-weight: 700;
    color: rgba(0, 0, 0, 0.4);
    margin: 20px;

`;

export default DrPlaceholder;
